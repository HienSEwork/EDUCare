server:
  forward-headers-strategy: framework

spring:
  jpa:
    properties:
      hibernate:
        jdbc:
          time_zone: UTC

management:
  endpoint:
    health:
      probes:
        enabled: true
